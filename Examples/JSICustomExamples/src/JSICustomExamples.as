package {
	import flash.display.Sprite;

	/*
	 * ANY CLASS IN THIS PROJECT IN ROOT PACKAGE IS A SINGLE EXAMPLE. SETUP IT AS APPLICATION 
	 * AND RUN UNDER HTTP PROTOCOL(some examples will NOT work locally).
	 * 
	 * Some of examples in this project has different HTML template with example JavaScript code.
	 */
	public class JSICustomExamples extends Sprite{
		public function JSICustomExamples():void{
			
		}
	}
}
