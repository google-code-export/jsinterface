package{
	/*
	 * ANY CLASS IN THIS PROJECT IN ROOT PACKAGE IS A SINGLE EXAMPLE. SETUP IT AS APPLICATION 
	 * AND RUN UNDER HTTP PROTOCOL(some examples will NOT work locally).
	 * 
	 * ALL examples in this project has own HTML template with example JavaScript code.
	 */
	public class JSIJavaScript extends Sprite{
		public function JSIJavaScript():void{
			super();
		}

	}
}